import React, {Component} from 'react';
import PhotoDetail from '../PhotoDetial'

export class PhotoDetailContainer extends Component {
  constructor(props) {
    super(props);
    this.state = {  }
  }
  render() { 
    return (  )
  }
}